#! /usr/bin/env python
# encoding: utf-8
import os
import wafbuilder as builder

def build(bld):
    name  = builder.appname()
    pysrc = bld.path.ant_glob('**/*.py')
    builder.checkpy  (bld, pysrc)
    builder.copyfiles(bld, builder.appname(), pysrc)

builder.make(locals())
